# libresessionflow-core (Grok plugin stub)

Bundles core libresessionflow skills for install-from-path.

v0: skills live primarily under repo `skills/` — copy or symlink into this plugin's `skills/` when packaging.
